@model VacancyResponse

@{
    ViewData["Title"] = "Удаление вакансии";
}

<h1>Удаление вакансии</h1>

@Html.Partial("_ViewVacancy", @Model)

@Html.Partial("_BackNavigation")

<form asp-action="Delete" class="d-inline">
    <input type="hidden" asp-for="Id" />
    <button type="submit" value="Delete" class="btn btn-primary btn-danger">Подтвердить удаление</button>
</form>