@model string

@{
    var linkValue = Model;
    if (linkValue == null)
    {
        linkValue = "Назад к списку";
    }

    <a asp-action="Index" class="btn btn-secondary">&lt;&dash; @(linkValue)</a>
}